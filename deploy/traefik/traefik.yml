entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"

api:
  dashboard: false

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO

tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/origin/cert.pem
        keyFile: /etc/traefik/origin/key.pem
  certificates:
    - certFile: /etc/traefik/origin/cert.pem
      keyFile: /etc/traefik/origin/key.pem
